{
  "name": "riviggy",
  "version": "1.0.0",
  "description": "Campus Food Ordering Platform with Group Orders",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "bootstrap": "npm run install:all && node scripts/ensureEnv.js && echo Bootstrap complete",
    "seed": "cd server && npm run seed",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client start",
    "predev": "node scripts/killPorts.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
